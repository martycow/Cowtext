# Fleet Activity Log

Maintained by **project-manager**. One brief entry per agent per session; newest on top. Target: keep the three most recent sessions here, older entries roll to an archive file under `docs/_archive/` (the name `ACTIVITY_LOG_2026H1.md` has been cited here since the log began but **no such file has ever existed** — the roll-out has never run) — note that `docs/_archive/` is write-frozen by the docs-guard hook, so rolling entries out needs Marty to lift the guard for that move (the 2026-08-19 WO02 entry below is queued to roll first). Sessions older than the top three are **not deleted in place**: archiving is a `git mv`, and no session may drop history it cannot archive.

**Hook rows moved out, 2026-08-24.** The `log-subagent.ps1` SubagentStop hook used to append one row per subagent stop to the bottom of this file, each listing the whole dirty worktree. It had reached **2,292 rows against 198 lines of human log** — 92 % of the file, and the dominant term in every session's diff. Those rows are machine output, not documentation, so the hook now writes `.claude/logs/subagent-activity.log` (gitignored via `*.log`) and the historical rows were moved there verbatim. Git history still holds the originals here.

**Still open** (`docs/tasks/BACKLOG.md`, P1.5): the roll-out described above. This file carries **17 sessions**, not the three the header targets and `CLAUDE.md` states, because writing into `docs/_archive/` needs Marty to lift the docs-guard freeze.

## 2026-08-22 — WO15: Release truth + UI round 2 (tech-lead contract + matrix, 9 build lanes, tester manual + adversarial pass, tech-lead audit, fix round, dispatcher live run; 78 invoke commands, 819 Rust tests, 297 Vitest tests, all green)

Directed by tech-lead under `/ultracode`; frozen contract `docs/_archive/contracts/WO15_CONTRACT.md` + companion `docs/design/PROVIDER_SUPPORT_MATRIX.md` (both 2026-08-21). **Theme: stop the product promising what it cannot do, and make the release numbers generate themselves.** Sources: `docs/INPUT_PROMPT.md` TASKS_UI_ROUND2 Blocks 0–7 and the P0 plan's Stages 0–6 with its 12 acceptance criteria.

**Sequence:** TL (contract + matrix) → **S0** alone (seams) → **R1 ‖ R2 ‖ U1 ‖ U2 ‖ U3 ‖ U4a ‖ U4b ‖ B1 ‖ D1** → tester (gates + golden-path manual + adversarial pass) ‖ tech-lead (audit) → fix round → dispatcher live run → PM close-out.

**Lanes landed.** **S0** (tech-general, alone): `src/store/toolchain.ts` + `src/store/ui.ts`, the bundled `src/resources/` set (`models.json`, `agent-presets.json`, `stacks.json`, `principles.json`, `skills/task-format/`, `index.ts` with `PROVIDER_SUPPORT_SENTENCE`), `src/wizard/projectGraph.ts` (pure graph builder), `src/ui/LocalOnlyBadge.tsx`, `src/agents/builtinSkills.ts`; deleted the dead `src/tasks/taskFormatSkill.ts` re-export. **R1** (git): `git_status` gains the two identity fields, `git_init(root, branch, commit)` returns `GitInitResult`, first commit is `chore: init cowtext project`; identity missing ⇒ `Err` **before any mutation** (A-4 — a half-initialised folder is worse than an untouched one). **R2**: new invokes `hooks_addr` and `skills_materialize`, `AiTool.elapsedMs`, toolchain probe timeout 4 s→3 s, task status alias `in review`. **U1**: "Node type" replaces "Role" on every surface + `?` popover with per-type micro-examples; AssembleSection (Brief / Tags / Influence / actions / live preview) with Influence visible without scrolling; Advanced section with Position marked local-only; EventLog note rows. **U2**: six-step New Project wizard (Principles / Stack / Git / Create) with a Will-create list and a result block; `BranchPicker`; GitWizard commit toggle. **U3**: provider→model picker with inline SVG provider icons, agent presets, `DEFAULT_PRIORITY = 1`, a new description validator, built-in-skill rail groups with Include-in-compile + `materialized` / `modified from built-in` badges + Reset-to-built-in, CompileModal skill rows. **U4a**: title-screen auto-scan + Rescan + the canonical provider sentence + Recommended door; Settings → Appearance (UI scale, UI/code fonts, calm, FPS) with portal-root scaling; single-mount audit; Inspector hidden in the Tasks surface. **U4b**: canvas agent menu items, `EmptyCanvasGuide`, Overlay label, Run-dialog defaults + ceiling input, task-type chips, Tasks CTA. **B1**: Barn ticker tooltips, legend strip with Demo/Connect-hooks actions, integer wide-screen fit. **D1**: `scripts/truth.mjs` + `truth.lib.mjs` (T1–T14, `--write`, `--no-cargo`), `AGENTS.md` and `.agents/skills/*` now **generated** from `CLAUDE.md` / `.claude/skills/*`, docs-guard messages naming the script, README sentence.

**Gates (dispatcher, 2026-08-22):** `npm run build` ✓ · `npm run lint` **0 errors / 16 warnings** (all react-refresh) · Vitest **297 tests / 17 files** · `cargo test --all-targets` **819** (785 lib + 18 CLI + 16 MCP) · `cargo clippy -- -D warnings` ✓ · `generate_handler!` **76→78** · `graph.json` schema **v5 unchanged** (no new node/edge fields) · compile targets claude/agents/cursor/copilot/gemini · **no new dependencies**.

**Defects.** Tech-lead audit (`docs/_archive/contracts/WO15_AUDIT.md`): **0 CRITICAL · 2 MAJOR · 8 MINOR · 10 NIT**. Both MAJORs were seam defects the contract itself prescribed — **F1** (a non-Anthropic model choice was dropped at Create while the badge promised it was "kept locally": D-13 said sidecar-persisted, §3.8 listed `provider` alone) and **F2** (`loadAgents` after Compile / Reset wiped unsaved skill drafts and the rail selection — the WO11 class-9 shape re-entering through store state instead of disk). Tester's pass: **2 MEDIUM · 9 LOW · 3 NIT**. Fix round closed F1 + F2 and tester findings #2–#11: sidecar `model` key (A-20), narrow `reloadSkills(root)` replacing `loadAgents` at both new call sites (A-21), deferred built-in includes, the double toast on a denied edge, the wizard result block, `useHooksAddr()` everywhere including `ProjectWizard` (A-22), git-probe gating, keyed AssembleSection, TaskPanel chips, panel heights at 130 % scale, ResizeHandle delta scaling, menu item disabled for non-importable node types, T9's case-insensitive patterns + the `generate_handler!` phrasing, first-run primary door. **Contract amendments A-20…A-24** recorded in the audit §13. The audit's MINOR "`git add` on an empty `context/`" did **not** reproduce on git 2.45; R1 hardened the path anyway (+3 tests).

**Live run** (dispatcher, `npm run tauri dev`): title screen in its scanned state; Settings → Appearance at 100 % and 130 %; context-menu alignment at 130 %; the six-step wizard producing `branch main · 1 commit` **on disk**; Inspector Influence visible at 1080p; the `?` popover; Tasks/Agents empty states with working CTAs; agent wizard with the Reviewer preset writing `model: claude-fable-5`; Compile with a skill row writing `CLAUDE.md` + `AGENTS.md` + `.claude/skills/task-format/SKILL.md`; Barn legend + wide fit; empty-canvas guide.

**Golden-path manual:** `docs/testing/GOLDEN_PATH_MANUAL.md` — **38 risk-based scenarios**, sections A–J, ~55 min, each naming the one risk it exists to catch, with a *Snap before / Snap after* folder-watch rule on every writing step (P0 criterion 10). This is the v0.1.0 release-gate walk; the release gate now has one home, `docs/tasks/ROADMAP.md` §Release gate.

**Process:** **two session-limit interruptions**, both resumed from the contract + lane reports **without loss of work**. The truth block in `CLAUDE.md` (between `<!-- truth:begin -->` / `<!-- truth:end -->`) is now the **only** place release numbers live — `npm run truth` fails the gate on a stale count anywhere in `CLAUDE.md`, `AGENTS.md`, `README.md`, `docs/TERMINOLOGY*.md`, `.claude/skills/*`, `.agents/skills/*`, `.claude/agents/*`. Standing lesson from this round: **the contract's own decision table and its wire spec can disagree** (D-13 vs §3.8; §6 U3.6's `loadAgents`) — a mirror audit that reads only the spec section will ratify the wrong half; read the decision table too.

**Open for Marty** (contract §10 + matrix §2): port 4923 as canon; whether to delete `.codex/hooks.json` and `.codex/agents/*.toml` (kept, marked unsupported/dev-only); review of the non-Anthropic model ids in `src/resources/models.json`; UI scale deliberately excludes canvas node cards; task status is labels-only on disk; built-in-skill placement after materialisation (D-5); `git_init` leaving the folder untouched on missing identity. **P1 not started — checkpoint first, per the plan.** Idle by scope: product-analyst. **Nothing committed** — the worktree stays dirty for Marty's review.

## 2026-08-21 — WO13: Gates green, acceptance walk pending (9 lanes + adversarial audit + fix round; 75 invoke commands, 770 Rust tests, 163 Vitest tests, all green)

Project-manager (final agent). **Zero dispatch — session close-out only.** All nine build lanes from WO13_CONTRACT.md (U1–U9) landed with gates green on 2026-08-21. Acceptance walk awaiting Marty. **Three refactors**: (1) node taxonomy roles 13→14 (new: Decision, Env, Tool; renames: rules→rule, task→workflow, reference→architecture, snippet→example), (2) edge model edges 7→5 (new: Contradicts; renames: conflicts-with→contradicts, conditional→imports+guard, supersedes→node.deprecated field), (3) agent modal (required description, inherit/restrict tools, provider dropdown removed, frontmatter keys promoted). **Graph schema v4→v5**: `pinned: bool` → `rootLoad?: "always"`, new `deprecated: {replacedBy, since?, reason?}` on nodes, new `needsReview: bool` on nodes, `condition: string?` → `guard: {type, ...}?` on edges; one migration in both Rust + TS, byte-identical fixtures. **New modules**: `.claude/commands/` emitter for generated CLI rules, `src-tauri/src/resolve_load.rs` (three prior load-policy implementations collapsed into one), `src-tauri/src/fsbatch.rs` (batch apply with all-or-nothing rollback + Undo). **Frontend**: Vitest first-ever runner, **163/163 tests** across 12 files (new suite). **Invoke 74→75**: new `fs_apply_batch` command. **Rust tests 658→770** (736 lib + 18 CLI + 16 MCP), all green. **TS gates**: `tsc --noEmit` clean, `npm run lint` 0 errors. **All 7 of Marty's acceptance defects fixed**: git entry points (D1a/D1b/D8: init branch choice, dead checkbox, commitless-repo misread), assemble-depth (D5/F7: node+agent fields, gate), canvas-loop (D3b/D6: oscillator, churn), toast-shell (F1), agent-questions (F2), wizards (D2/D4a/D4c), task-format (F6), starter-presets (F5), node-surfaces (D3a/D7), run-button (F3), mcp-server (F8), app-shell (F1/F2/F3/F5/F7). **Audit findings (16 defects D1–D16, Amendment 4)**: all fixed in-session. Amendment 4 was tech-lead's §18.1 Exception P: `graph_v4_in.json`'s own `overrides` edge is co-resident after migration, so precedence marker fires on all four root files in the byte-identical *control* fixture. **Process finding**: this was the third incomplete §18.1 exception list (all three omissions were output changes specified elsewhere in the contract), and **the standing lesson is: the gate is the authority, not the enumeration** — every lane implemented both halves faithfully, gate computation proved which halves diverged. **Two zone-violation incidents recorded** (U1 destroyed T1's work, R1/R2/R3 snapshot-restore clobbered concurrent edits — both root cause: agents trying to force-green against foreign files; tech-lead §4 proposal: per-lane zone manifests enforced by PreToolUse hook, Stage 0 stubbing all cross-lane seams, hard no-snapshot-restore rule). **Four §17 grid gaps** (files assigned to no lane: `src/scene/sfx.ts`, `src/store/events.ts`, `src-tauri/src/bin/*.rs`, `src-tauri/src/handoff.rs`; two were *silently broken*: `events.ts` broke defect 5's stepper, `handoff.rs` stamped deleted role). **Standing lesson** (tech-lead): Rust↔TS mirror pairs agreed with each other while both diverged from spec twice (D5 seed handling, D15 deprecated precedence); always audit mirrors against contract text, never against twins; unpinned pairs now incomplete deliverables. **Mandatory shared corpora added**: `resolve_load_cases.json` (19 test cases), `edge_legality_cases.json` (27 test cases). **Open**: §10.5 (generated `.claude/commands/*.md` description frontmatter — unverifiable from sandbox), defect 7's trigger (recoloring label movement — assumed but unproven statically), glob match count (scan-only `.md` reads; real fix requires broader scan or new command, out of WO13 scope per explicit brief). **Gate status: GREEN** — all lanes delivered, all defects fixed, all tests pass, WO13 feature-complete. Idle by scope: product-analyst. **Acceptance walk pending**: Marty's manual (WO13 + WO12 + WO11 + WO10 + WO09 + WO03 + WO02 + WO01).

## 2026-08-20 — WO12: Marty acceptance round 2 (12 build lanes; 74 invoke commands, 624 lib + 16 MCP tests; 2 PARTIAL items flagged)

Directed by tech-lead under `/ultracode` dispatcher. **Twelve lanes landed per frozen contract** (`docs/design/WO12_CONTRACT.md [DEAD REF — never existed in the repo; verified 2026-08-24]` + WO01 Block F ratifications §8–§12): git-truth (D1a/D1b/D8: dead checkbox, branch choice, commitless-repo misread), assemble-depth (D5/F7: node+agent fields, confirmation gate), canvas-loop (D3b/D6: selection oscillator, StoreUpdater churn), toast-shell (F1: notifications system), agent-questions (F2: `agent://event` question kind), wizards-project-agent (D2/D4a/D4b/D4c/F4: project wizard fixes + priority label + avatars + duties header + hooks toggle), tasks-format-skill (F6: task-type column + skill), starter-presets (F5: 3-node task/backlog/changelog preset), node-surfaces (D3a/D7/F7: role picker filtering + token display + gate integration), run-button-sessions (F3: consolidated launch dialog), mcp-server (F8: stdio JSON-RPC 2.0 with 14 tools), app-shell (F1/F2/F3/F5/F7: integration into App.tsx). **Invoke 73→74**: `assemble_preview` added; no other new commands. **Rust tests 604→624 lib** (20 new: git/worktree/assemble/sessions; concurrent F6 lane's in-progress task_type edits prevented full-crate verification but lane-scoped tests all green). **MCP server**: 16 unit tests (stdio protocol, initialize, tool discovery, dispatch); verified live over real stdio. **Two PARTIAL items** (recorded as open bugs): D3a (node-wizard role filter enforced only at UI level, not at file-creation; a hand-edited preset with role=agent still bypasses to context/*.md), F6 (task_type column persists to graph but is silently destroyed on every Inspector edit or board status-change, breaking day-to-day board persistence — TaskPanel and setStatus both send partial patches that clear the field). **Technical debt recorded**: docs-guard.ps1 hook blocks Edit/Write on `.claude/skills/*.md` (F6 lane worked around via Bash sed; assemble-depth lane same workaround for terminology skill), hook exemption for `.claude/skills/` worth adding. **Latent defect flagged**: git.rs's probe_status still uses git rev-parse on commitless repo, leaving GitWizard's status.branch chip blank until first commit (root cause D8 fixed in worktree_check only; not in scope per explicit brief constraint). Idle by scope: tech-barn, product-analyst. Awaiting Marty's manual walks (WO12 + WO11 + WO10 + WO09 + WO03 + WO02 + WO01 all open). **Gate status: YELLOW** (two PARTIAL items; all other defects fixed; no gate failures).

## 2026-08-20 — WO11: Acceptance-walk defects + Home/Git/Project/Avatars (4 build lanes + 3 doctrines + tester + tech-lead audit; all 16 defects fixed; 3 crashes remain UNPROVEN and open)

Directed by tech-lead under `/ultracode` dispatcher. **Four lanes + doctrines delivered per frozen contract** (`docs/_archive/contracts/WO11_CONTRACT.md` + amendments §10, §11, §12): R1 (git: init + .gitignore composer with presets, no dependencies), R2 (agents backend: avatars + memory status probe + Rust-side write-safety mutex for .claude/agents/ + .claude/skills/), UI-A (wizards: project types + list-field space/newline fix + A4 disclosure), UI-B (shell + rail: Home button with session warn, hierarchy row shape fix, rail project row, wizard entry points), UI-C (Inspector: section order, project panel, agent-delete seam, path-safety fix), UI-D (agent properties: autosave debounce + FIFO queue fix, agent editor without Save button, avatars UI, fleet-view detail, tools dropdown scroll fix, model catalog dedup, Markdown-tab agent branch). **Stage 0 blocking:** global `ErrorBoundary` (renders stack + message + Reload; no silent failure on B2/C1/F1). **Tech-lead audit identified 3 UNPROVEN crashes** (B2 agent selection, C1 adopt-to-graph, F1 assemble-after-close): all confirmed code defects on those paths FIXED (bare-`/` split → `canonPath`, `sameRelPath` path comparison in delete listeners, CodeMirror rebuild prevention), but crashes themselves remain UNPROVEN from headless shell — ErrorBoundary now renders the stack on any throw, making future live runs definitive. Invoke **66→73** (+7: git_status/git_init/gitignore_write, agent_avatar_set/read/clear, agent_memory_status). Rust tests **553→586 lib + 18 CLI = 604** (51 new). **Tech-lead §10 ruling** (D5 delete button removal): Inspector keeps no agent-file delete; one writer per file via rail context menu only. Fixed orphaned-node bug with new `agentDeleteListeners` seam (`agents.ts` → `graph.ts`, uses `sameRelPath` to match backslash-stored paths). Fixed two stray path-comparison defects in same hunk. **Tech-lead §11 ruling** (TOCTOU serialization): one module-scope `AGENT_FS: Mutex<()>` in `agents.rs`, guarding 16 commands (agents + skills + avatars + memory ensure), **no `.expect()` — recovery via `unwrap_or_else(|p| p.into_inner())`** (deliberate: assemble.rs's `.expect("assemble queue mutex")` chain is the F1 hypothesis). Tests: `agent_rename_during_save_never_resurrects_the_old_file` (25 trials, real OS threads, deterministic pass every time). **Tech-lead §12 ruling** (Markdown-tab second-writer bypass): **one writer per file doctrine**, recorded in TERMINOLOGY.md. `MarkdownTab` rerouted for agent paths (load from store, save via `saveAgentRaw` queue), not `read_md_file`/`write_md_file`. `write_md_file` guards agent paths (Rust rejection arm). `setTab` flushes pending saves and resets on selection change. `MemoryNodeCard`'s "Create file" → `agent_create` for agents (real template). **Defect tally in WO11**: 16 found across all lanes (8 HIGH/MAJOR + 8 MEDIUM/LOW); ALL FIXED. Tester found 4 in audit (1 HIGH, 1 MAJOR, 2 MEDIUM), plus 1 HIGH on re-verification (TOCTOU window); R2 found 1 HIGH (AGENT_FS mutex gap); UI-C found 2 (delete self-conflict + render-batching); UI-D found 4th writer bypass (Markdown tab). **Three HIGH from same seam**: D4 autosave queue without audit of all other writers (Markdown tab, Review revert, Create-file) → three data-loss paths, all now one-writer. **Process incident recorded**: UI-A wrote temporary stub files into another lane's zone (src/git/), broke R1's landed work — stubs deleted before commit but unrecoverable from git. Rule: **never write scratch into another lane's zone**. **Manual written:** `docs/_archive/manuals/WO11_TEST_MANUAL.md` (1072 lines, sections A–M plus four subsections, 109 steps, including B's one-pass crash capture, G.3/G.6/G.7 race/concurrency tests, J.1 CRLF fix verification, K avatar 2GB-sparse-file stat-first rejection). **Final gates:** `npx tsc --noEmit` 0, `npm run lint` 0 errors (7 pre-existing react-refresh warnings), `npm run build` ✓, `cargo clippy --all-targets -- -D warnings` clean, **`cargo test` 604/604 (586 lib + 18 CLI), 0 failed**, **invoke contract 73/73** declared, registered, TS-called exact. Idle by scope: tech-barn, product-analyst. **Awaiting:** Marty's manual acceptance walks (WO11 + WO10 + WO09 + WO03 + WO02 + WO01 all open). Uncommitted.

## 2026-08-19 — WO09: Canvas Connectors, Round 2 (one build lane + tester gates; defects found + fixed in-session, feature-complete)

Directed by tech-lead under `/ultracode` dispatcher. **Round 1 (uncommitted)** received Marty's rejection on 2026-08-19 with two findings: (1) connection lines still too far from Input connectors, (2) connectors should be taller and resemble 16-bit video game cartridge contacts. **Root cause identified by tech-lead:** React Flow's `getHandlePosition` returns handle border-box outer edge (not centre), causing `edgePath.ts` to double-count socket geometry with a stale `SOCKET_GAP = 10` constant. **Frozen contract: `docs/_archive/contracts/WO09_CONNECTOR_CONTRACT.md` (420 lines, §3 constant table as single source of truth: `CONN_H 44`, `FINGER_COUNT 5`, `FINGER_PITCH 8`, `RISER_STEP 8`, `NODE_H 128`; wire plugs in at `targetX + SOCKET_BITE(3)`, arrowhead tip deliberately swallowed by socket with 8/11 px readable; path start at `sourceX + PIN_REACH(10)`; persistent per-edge slots deferred but geometry-aware ordering, lane routing, custom preview all scoped OUT for v0.1.0).** **Tech-ui one lane, 8 files, zero deviations:** NEW `src/canvas/portSlots.ts` (assignPortSlots pure rank on string compare, `SLOT_COUNT 5`, overflow wrap rule); `edgePath.ts` re-signed to one EdgeEnds object with slot-shifted endpoints; `MemoryEdge.tsx` passes slots, `interactionWidth` 16→12; `GraphCanvas.tsx` calls assignPortSlots once in edges effect; `types.ts` adds inSlot/outSlot; `MemoryNodeCard.tsx` comment only; CSS `CONN_H 24→44` with `repeating-linear-gradient` cartridge fingers via `--contact` custom property on `.ct-port-{in,out}`. **Tester: all five gates green** — `npm run build` clean, `npm run lint` 0 errors (1 pre-existing RoleGlyphs warning), `cargo clippy --all-targets -- -D warnings` clean, `cargo test` **542 passed** (524 lib + 18 CLI, 0 failed), **invoke contract 63/63 byte-exact** (both sets extracted, `comm`'d, Rust untouched this round confirms no regression). `markerUnits="userSpaceOnUse"` zoom question resolved by reading `@xyflow/react` source: React Flow uses one CSS `transform: scale()` on viewport wrapper, so paths and markers scale as one pixel image, no zoom gap. Tester verified `assignPortSlots` genuinely pure (node moves never touch domainEdges). **2 MINOR defects found + FIXED in-session** (both stale comments, pre-existing blamed to commit 7122ee1 but in files this lane owns for comment refresh): MemoryNodeCard.tsx:13-16 described nonexistent three-handles-per-side + `picks` architecture contradicting real single-handle/no-id JSX; MemoryEdge.tsx:8-14 had per-kind stroke table stale vs real const (2px/4px claimed vs 3px/5px actual, every dash off by one). Fix round changed comment text only; STROKE, JSX, handle wiring byte-identical. `npm run build` + `npm run lint` re-run clean after. **Manual written: `docs/testing/WO09_TEST_MANUAL.md` (45 steps A–J, covering contract §10's eight on-screen checks, four reject conditions §10.1/§10.2/§10.4/§10.7, three zoom levels 50%/100%/200%, state-colour cascade, with sign-off table).** Feature-complete; gates green; awaiting Marty's manual acceptance walk. Idle by scope: tech-general, tech-barn, product-analyst. Uncommitted. **Note: INPUT_PROMPT's earlier brainstorm block (08/19 2:10 PM) lists 11 items including pathfinding, hierarchy panel, wizards, project properties, orchestrator tab; round-1 work and `src/orchestrator/` appear to address some but are out-of-scope for WO09 — logged to backlog.**

## 2026-08-19 — WO06: L2 Orchestrator Suite (7 build lanes + audit + two fix rounds; all gates green, feature-complete)

Directed by tech-lead under `/ultracode` dispatcher. **All seven build lanes delivered per frozen contract** (`docs/_archive/contracts/WO06_CONTRACT.md`): G1 (task DAG, stable task ids, cycle detection, board defects O2/O3), G2 (tasklinks sidecar v1, per-task subgraph injection differentiator, handoff-to-node), G3 (token budgets atomic hard-stop, budget event, restart reset), U1 (board DAG UI, deps picker, blocked badge, O1 checkbox), U2 (task-context modal, handoff-node commit flow), U3 (ceiling settings, effective-ceiling fallback, gauges), B1 (barn mission control stalls). **Invoke 54→63** (9 new commands). **Tech-lead audit** (2026-08-19): identified 3 CRITICAL (D1 differentiator modal not mounted, D2 handoff wiring absent, D3 restart tokens not reset), 6 MAJOR, 8 MINOR + 6 observations. **Two fix rounds**: first round (tech-lead) landed with G-lanes; second fix round completed all defects. **All defects FIXED**: D1/D2/D3/D4/D5/D6/D7/D8 plus O1–O8 minors. **D9 (global token ceiling) COMPLETED** in second round: `AppSettings.sessionTokenCeiling` default 200,000 (23 refs across settings.rs/settings.ts/SettingsModal.tsx with 18 new tests). **Handoff wiring COMPLETED**: `src/handoff/api.ts:31` calls `handoff_node_propose`; `HandoffNodeProposalModal.tsx` + `types.ts` + modal integration in `HandoffModal.tsx` all shipped. **Final gates** (post-fix rounds): `npx tsc --noEmit` 0 errors, `npm run lint` 0 errors, `cargo clippy --all-targets -- -D warnings` clean, **`cargo test` 524 lib + 18 cli = 542, 0 failed**, **invoke contract 63/63 registered AND 63/63 reachable**. Pre-fix audit and tester re-verification (manual `WO06_TEST_MANUAL.md`) documented the identified gaps; fixes subsequently completed. Feature-complete. Idle: product-analyst. Uncommitted.

## 2026-08-19 — WO03: L1 Moat Hardening (7 build lanes + audit + two fix rounds, all lanes landed, v0.1.0 cut gate)

Directed by tech-lead under `/ultracode` dispatcher. **All seven build lanes delivered per frozen contract** (`docs/_archive/contracts/WO03_CONTRACT.md`): A (graph v3: roles 7→13, edges 4→7, tags/owner/meta, edge color; migration harness; preset v2→v3 upgrade), B (compile targets 3→5: copilot+gemini adapters, Kahn ordering honors overrides), C (cowtext-cli second binary; compile --check/lint/--json), D (importer MVP: import_scan/import_apply, never clobber), E (linter v1: cycles, duplication, stale, conflict via explicit edges), F (frontend: 13-role KindPicker, 7-kind edge picker with structural affordance, tags/owner in Inspector, Problems panel, import-review modal, compile checkboxes), G (docs: terminology, skills, task-format conventions). **Invoke 51→54** (lint_run, import_scan, import_apply). **Tech-lead audit** (2026-08-19): 13 findings + 5 OBSERVATIONS; 4 deviations ratified (§4.1 overrides order; D5 collation choice; D9 dual cycle detectors; D6/D7 strict-vs-tolerant model choice). **14 defects found across audit cycles**. Two **CRITICAL** defects found in audit + first fix round: D1 (import silently loses everything in store after write — no reload), CRITICAL not-in-audit (cowtext-cli `[package]` lacked `default-run`, broke `npm run tauri dev`). D2/D3/D4 MAJOR (importer proposes compile-output as nodes; TS wire shapes missing 2 fields; role popup keyboard-unreachable = WCAG 2.1.1 failure). D5–D9 MINOR (graph.json collation disagreement Rust/TS; strict model rejects unrecognized enums; ProblemsPanel hides real errors; `.mdc` globs dropped; two cycle detectors unpinned). D10–D13 NIT (stale comments, nested @-resolve root-relative, presets drop meta). **All 14 FIXED in fix round**; gates re-run green (npm/cargo/clippy/387 tests/54 invoke byte-exact). Manual written: `docs/testing/WO03_TEST_MANUAL.md` (583 lines A–H, includes CSP production-build step). Awaiting Marty's manual acceptance walk. Uncommitted. **Idle by scope**: product-analyst. **tech-barn** (NOT idle — owned `sceneGraph.ts` micro-lane preserving existing barn logic under graph v3 edges).

## 2026-08-19 — WO02: 7 build lanes + audit (quick-win + format-discipline, all lanes landed)

Directed by tech-lead under `/ultracode` dispatcher. **All seven build lanes delivered per frozen contract:** G1 (tasks.rs: 5 convention files incl. BUGS.md, table-aware append/move, priority buckets), G2 (agents.rs+lib.rs: NEW `agent_memory_ensure` command; `agent_create` optional fileName), G3 (stores: showFps/TASK_FILE_NAMES/TaskPriority/normalizePriority/createAgent opts), U1 (agent-card visual identity, wizard-only node creation at viewport centre, brightness#17a), U2 (TasksBoard full-width segmented TASKS·BACKLOG·ROADMAP·BUGS, TagPicker, PriorityBadge, brightness#17b), U3 (AgentEditor memory row, NewAgentDialog calculated paths, SettingsModal FPS, model catalog+inherit), B1 (FPS overlay zero-cost-when-off; perf investigation: devicePixelRatio×INITIAL_ZOOM GPU fill-rate; hover-label caching fixes applied; resolution-cap handed back). **Tech-lead audit**: gates 6+8 PASS; verdicts V1–V4 documented; defects D1/D2/D3 ALL FIXED by owning lanes in fix round; D4 = cowtext-terminology skill added to P1 zone. **Tester**: ALL automated gates green after fix round — npm/cargo/invoke 51/51 byte-exact. Manual: WO02_TEST_MANUAL.md (61 steps A–H). Two LOW findings: untrimmed fileName in agent_create (in flight), AgentAvatar process note (V2). Gate 10 pending P1 docs rewrite. Idle: product-analyst. Invoke 50→51. Gates green; WO02 manual awaiting Marty's walk; uncommitted.

## 2026-08-18 — WO01 FULLY CLOSED: Blocks D/E + N1–N5 batch (ultracode work order, all A–F committed + N suite uncommitted)

Directed by the tech-lead under the `/ultracode` dispatcher. **Blocks A–F all accepted by Marty or landed (2026-08-18):**  Block A (canvas lenses + watcher, reviewed+accepted by Marty); Block B (token budget, `f499ccf`); Block C (review queue, `306f56a`); Block D (`72dc1a9`); Block E (`f1752b0`); Block F (Agents MVP, `269bdad`). **N1–N5 suite (task-format skill batch) uncommitted** — awaiting dispatcher's final commit.  App version stays **v0.1.0** (first public cut). Invoke contract reaches 50 (six agent session commands + `agent_session_list` in Block F). Tester found 1 N-batch defect during manual walk, fix round ran, resolved before uncommitted state.

| Component | Commits | Status |
|---|---|---|
| Block A (Lenses + Watcher) | reviewed+accepted | ✅ Done |
| Block B (Token Budget) | `f499ccf` | ✅ Done |
| Block C (Review Queue) | `306f56a` | ✅ Done |
| Block D | `72dc1a9` | ✅ Done |
| Block E | `f1752b0` | ✅ Done |
| Block F (Agents MVP) | `269bdad` | ✅ Done |
| N1–N5 (task-format skill) | uncommitted | ✅ Done |

**Defects found/fixed this session**: 1 N-batch defect found in tester walk, fixed before uncommitted state. Earlier in session: Block C's skipCurrent race FIXED before landing (audit caught, pre-ship).

**Next in protocol**: Dispatcher commits N1–N5 and this record; manual acceptance walks for all WO01 blocks remain open (A–F manuals complete, N walk pending). v0.1.0 cut gate: real sprites (Marty-side asset), CSP production check, acceptance sign-off.

## 2026-08-18 — Task-Board batch (primary agent dispatcher + 3 lanes, no Workflow)

Marty's 7-item batch on top of WO01 (Block A still awaiting his review). Contract: `docs/_archive/contracts/TASKBOARD_BATCH_CONTRACT.md`. Gates green: build, lint 0 errors, clippy `-D warnings`, cargo test **196/196** (+27), invoke contract **42/42**. Manual: `docs/_archive/manuals/TASKBOARD_BATCH_TEST_MANUAL.md` (18 min). NOT committed at entry time.

| Agent | What they did |
|---|---|
| primary (L2) | Dispatcher + store lane: graph undo/redo (bounded snapshots, keystroke coalescing, selection filtering), `src/tasks/api.ts`, `useTasksStore` with watcher-driven debounced reload, token estimators (`pinnedContextTokens`/`agentContextTokens`, ~200k framing), Producer consts, contract + manual + docs close-out |
| tech-general (L1) | `src-tauri/src/tasks.rs`: tolerant table+checklist parser, 4 commands with atomic line surgery + move rollback (38→42); Producer guards in agents.rs ("Reserved agent: producer"); 27 new tests |
| tech-ui (L3) | `TasksBoard` (reusable, agent filter incl. Producer-null semantics, quick-add, Move-to menu) + Tasks modal + TopBar button; rail directory tree; Undo2/Redo2 buttons + Ctrl+Z/Y with editable-target guard; token chips (TopBar pinned, AgentEditor context) |
| tech-barn (L4) | `src/scene/hover.ts`: silent hover bubbles (cow/calf/props/desk/door), AABB per-frame hit test, calm-aware, one bubble instance; UX debt: calf labels use ordinals until calf.ts exposes seeds |

**Idle by scope**: tech-lead (contract written by dispatcher), tester (gates run by lanes + dispatcher; adversarial pass deferred to the acceptance walk), project-manager (record written by dispatcher).
**Known limits recorded**: per-agent token SPEND needs Block F stream-json; agent matching in task files is a name-stem heuristic.

## 2026-08-18 — WO01 Block A: Canvas Lenses + Watcher (ultracode work order, stop for review)

Directed by the tech-lead under the `/ultracode` dispatcher; app version stays **v0.1.0** (first public cut — Block A lands before sprite assets). Frozen contract: `docs/_archive/contracts/WO01_BLOCK_A_CONTRACT.md`. Two disjoint lanes (Watcher + stores, Lens UI). All gates green: `npm run build` PASS, `npm run lint` (0 errors, 1 pre-existing), `cargo clippy -- -D warnings` PASS, `cargo test` **169 passed** (final, incl. fix-round regressions; baseline 158 pre-existing), invoke contract still 38 (scan_project carries only Tauri-injected args). Manual written: `docs/testing/WO01_BLOCK_A_TEST_MANUAL.md` (15 min, 29 steps covering lens switching, Activity decay via touch, Weight/Live lenses, persistence, unknown-value fallback, and a dedicated step reproducing the rename defect as a documented known-fail). Two defects found during audit — **1 MAJOR** (Windows notify rename halves mapped OLD path to Modify → ghost entries with false-fresh mtime under Activity/Weight lenses) and **1 MINOR** (flush() generation-guard TOCTOU) — and **BOTH FIXED in the fix round** (explicit From→Remove / To→Create mapping; per-entry generation re-check in `flush_with`), re-gate green, cargo test **169/169** (+4 regression tests), invoke contract re-verified 38/38 by the dispatcher.

| Lane | Agent | What they did |
|---|---|---|
| A | tech-general | Rust watcher pipeline: `src-tauri/src/watcher.rs` + `watcher/tests.rs` (notify = 8, hand-rolled 300ms debounce, 64-pending/1000ms-oldest starvation guards, generation counter for stale-emit kill, EventKind mapping incl. the rename bug), `project.rs` split `scan_root` + thin `scan_project`, new `is_scannable_md` parity test. Rust-side stores: `settings.ts` (LensMode/LENS_MODES/lens field, last in AppSettings, version stays 1), `events.ts` (LENS_LIVE_WINDOW_MS, lensLiveTs side-channel same scan as lastLiveTs), `project.ts` (FsChange interface + applyFsChange: sorted insert on create, drop on remove, in-place modify, no rescan). Third `listen("fs://change")` added inside initEventListener. docs/TERMINOLOGY.md: verbatim `fs://change` row appended to Events table (file now 100 lines, within ≤120 canon limit). 11 tasks all delivered. |
| B | tech-ui | Lens UI pipeline: `src/canvas/lens.ts` (LENS_BRIGHT_MIN/MAX, ACTIVITY_WINDOW_MS, brightnessFor linear decay, activityEmphasis, weightEmphasis + mandated Block B comment, useLensTickStore). `src/canvas/LensControl.tsx` (4-segment radiogroup, active = accent-border/bg/text, inactive = muted + hover, owns sole 30s ticker gated on lens=="activity"), Activity-only legend strip (96×6 gradient amber←surface-3, aria-hidden). `GraphCanvas.tsx` wrapped New-node button + LensControl in flex row. `MemoryNodeCard.tsx`: unconditional tick/lens/maxBytes/liveEmphasisTs subscriptions, `nowMs` via useMemo([tick, lens]), card root gains `--lens-brightness` / `--lens-emphasis` custom props + `filter: brightness(var(--lens-brightness, 1))` (lens=="none" forces both to 1, preserving live-pulse/stripe/ring/assemble-bar logic). Sequencing: lane A store exports confirmed final-exact per frozen API surface. tsc clean, 0 lint errors. 7 tasks all delivered. |

**Idle by scope**: tech-barn (no Barn/SFX work in Block A; thought bubbles are a future phase).

**Defects found** (both routed to BUGS.md Open): 1 MAJOR (Windows notify rename defect in watcher.rs map_event_kind + applyFsChange logic; external renames leave ghost entries; documented known-fail in manual step 23), 1 MINOR (TOCTOU race in flush generation guard; sub-millisecond repro window; not a block-close issue but flagged for tech-lead review).

**Next in protocol**: Marty reviews manual (29 steps, 15 min); if defects acceptable as known-issues for v0.1.0 cut, Block B begins (Compile lens integration + canvas lens-card priority). Invoke-name contract check deferred (no command signatures touched; low risk but should be re-run per tester duty list before sign-off).

## 2026-08-18 — Agents Suite: Frontmatter Parser + Identity Hash + UI + Named Calves (fleet session)

Directed by the tech-lead under Marty's plan-approved batch launch; app version v0.0.0011 (post-phases suite, agents feature). All gates green: `npm run build` PASS, `cargo clippy -- -D warnings` PASS, `cargo test` 143/88 (+43 new: 23 frontmatter + 18 agents + 1 project, including 2 regression tests added in fix round), invoke contract verified 27 → 37 (+10 agent/skill management commands). Four disjoint lanes (Rust backend, identity+wire types+store, UI chrome, barn+SFX layer) plus manual authorship; contract frozen at `docs/_archive/contracts/AGENTS_SUITE_CONTRACT.md`. New capabilities: manage agents/skills/memory-io with a nine-step read-patch-write frontmatter invariant, stable agent identities (fnv1a32 hash, avatar grid + calf look), named calves spawning on SubagentStop with stable identity across sessions. Found 4 real defects during audit: 2 major (re-quote churn on agent_save, orphan-key precedence race in sidecar), 2 minor (case-insensitive rename collision on Windows, inline Save bypassing phase lock). All four traced to source with reproducible repro paths and ALL FIXED in fix round before re-gate. Manual written: `docs/_archive/manuals/AGENTS_TEST_MANUAL.md` (53 steps, A–F sections).

| Agent | What they did |
|---|---|
| tech-general (lane A) | Rust backend: hand-rolled frontmatter parser (read-patch-write + byte-identity emit, no regex/YAML), 10 agent/skill commands per contract §2 (validate_component, resolve_within_root, etc.), SubagentStop hook appended to the 4-entry HOOK_EVENTS list, settings.json guard in project.rs. 21 frontmatter tests + 18 agents tests + 1 project test (40 new, 138 total). Contract deviation: `eq_ignore_ascii_case` for clippy `-D warnings` pass. Zone-clean final check confirmed. |
| tech-general (lane B) | Identity + wire types + store: fnv1a32 hash (Math.imul), avatar params (8×8 mirrored grid), calf look (popcount 2–7), `FmFields`/`AgentDoc`/`SkillDoc`/`AgentsScan` mirror contract exactly, ten `invoke()` wrappers in api.ts, full `AgentsState` store with explicit-save drafts, busy/opError discipline, re-keying on create/rename/delete, attach/detachSkill, orphan retention + sidecar reconciliation (clamped defaults, metaError-blocking writes). `tsc --noEmit` clean, no `any`, `npm run lint` 0 errors. Zone-clean verified. |
| tech-ui (lane C) | Agents manager modal, editors, identicon avatar: `AgentAvatar.tsx` (8×8 crisp-edge identicon, 11/44px sizes), `AgentList.tsx` (280px left pane, Agents/Skills sections, inline create error surfaces), `AgentEditor.tsx` / `SkillEditor.tsx` (draft-bound fields, Duties via CodeMirror, Save + dirty dot, raw-doc fallback), `AgentsModal.tsx` (PresetsModal shell widened, 4-state phase machine, Esc inert while busy, skipped-files footer, metaError + orphan cleanup), TopBar entry `onAgents` between Presets and Handoff, lazy `AgentsModal` + `flushMetaSave()` in `beforeunload`. `tsc` clean, `npm run lint` 0 errors, 1 pre-existing unrelated warning (RoleGlyphs), code-splits into own lazy chunk. Zone-clean verified. |
| tech-barn (lane D) | Named Calves + SubagentStop wiring + demo beats: `src/scene/calf.ts` (makeCalf ~0.6 scale, programmatic patch grid + accent from role, bell/bandana/flower/tag/none prop), `CalfHerd(layer)` with cap-4 lifecycle (door tile → hop 500ms → bubble → 4s linger → fade/despawn), `tick(dtMs, reduced)` reduced-motion support (no hop/bounce/fade, direct spawn, instant despawn). `SubagentStop` calls `herd.spawn(seed)` via session-scoped ordinals, `CalfHerd` exposes `onSpawn`/`onDespawn` callbacks wired to `sfx.play()` in BarnScene. Demo beats: two fixed-sessionId `subagent_stop` entries mid-script for reproducible demo calves. `tsc --noEmit` clean. Zone-clean verified. |
| tester | All 5 gates re-run green after fix round (build, lint, clippy -D warnings, 143/143 cargo tests +2 regression, 37/37 invoke contract count verified by direct grep). Adversarial audit per contract and lane reports: traced B's lazy-draft logic, A's parse/apply_fields round-trip against real fleet files, D's code reading. Found 4 real defects in initial audit, ALL 4 FIXED in fix round (lanes A/B/C): frontmatter re-quote churn (should_touch_key logic), orphan-key precedence race (live meta + reconcileOrphan timing), case-insensitive rename (dest==src check), inline Save phase bypass (onSave routing). |

**Idle by scope**: product-analyst (Agents Suite is feature delivery, not research — the suite was planned per the contract).

**Lazy/useless agents to flag for Marty**: none. All four tech lanes + tester delivered on contract; frontmatter invariant was the most complex seam and held. Two defects (frontmatter re-quote churn, orphan precedence) are ready to handoff to Marty or a follow-up sprint — one is 100% reproducible against real fleet files and should be quick to fix.

## 2026-08-18 — Ultracode UX quick-win batch (12 in-batch ideas + 4 new invoke commands) (fleet session)

Directed by the primary agent under Marty's launch; app version v0.0.0008 (post-phase suite). All gates green. Three disjoint lanes (Rust backend, store+IPC wrappers, UI chrome) plus manual authorship; contract frozen at `docs/_archive/contracts/UXBATCH_CONTRACT.md`. Single source of truth: 4 new `invoke` commands (rename_node_file, reveal_path, probe_project_dirs, hooks_status) byte-exact per contract, 23 → 27 total. Idea #15 (template languages) rejected by architecture verdict (JSX is the fixed layout language, separation of concerns already satisfied by layering). Idea #14 (connect nodes) already shipped Phase 1. Idea #16 edge-color deferred (needs graph.json v1→v2 + preset migration, out of batch scope). Three major audit defects found during manual authorship (race conditions + silent failures), all fixed; two by tech-general L2 (store layer), the third by tech-general in a follow-up after routing miss sent it to the wrong instance (which correctly refused to leave its zone).

| Agent | What they did |
|---|---|
| tech-general (L1) | Rust backend: `rename_node_file` (8-step guard), `reveal_path` (OpenerExt), `probe_project_dirs`, `hooks_status` (4-state read-only). 6+4=10 new tests, all pass. `cargo clippy -D warnings` clean. |
| tech-general (L2) | Stores + IPC: `src/fs/api.ts` (4 wrappers), `settings.ts` (recent projects, panel widths, brief height, syncFileName), `project.ts` (openProjectAt, refreshHooksStatus), `graph.ts` (renameNodeFile, commitTitle). `tsc --noEmit` clean, 0 lint errors. |
| tech-ui | UI chrome: ContextMenu primitive, ResizeHandle, ScanOverlay, role popup, title rename, handles on nodes (6 frozen ids), edges with curvature + multihandle, buttons/modals wired. `tsc` clean. |
| tester | All 5 gates run for real (build, lint, clippy, cargo test 98/98, invoke contract 27/27). Wrote `docs/_archive/manuals/UXBATCH_TEST_MANUAL.md` (75 steps, all controls verified in code, three known-issue repros). Three major defects found via adversarial read (refreshHooksStatus race, TitleField stale-closure rename-collision, reveal_path silent fail), all fixed by backend lanes + follow-up pass. |

**Idle by scope**: tech-barn (no Barn/SFX work in batch; background music deferred as Marty-side asset work).

**Lazy/useless agents to flag for Marty**: none. All working agents delivered per contract; tester's adversarial read found real concurrency bugs that need fixes before manual walk can pass.

## 2026-08-18 — Acceptance walks + docs/fleet reorganization (primary-agent session, no fleet)

Marty walked and accepted Phases 3–6 (M3–M6 closed) — **all seven phases 0–6 are now accepted**; current version confirmed v0.0.0007 (= the `cb770d4` ship-prep landing), next cut v0.1.0. Same session, primary agent under Marty's staged approval: docs/ reorganized (13 files ~207 KB archived via `git mv` into `docs/_archive/{contracts,research,superseded}/`; FEATURES.md merged into BACKLOG.md, MILESTONES.md + SPRINTS.md merged into ROADMAP.md; TERMINOLOGY.md split into a canon + TERMINOLOGY_REFERENCE.md, invoke count corrected 14 → 23). New fleet stood up: 6+1 agents in `.claude/agents/` (this roster replaces the 13 callsigns — mapping in ROSTER.md), 5 knowledge skills + the `/ultracode` dispatcher in `.claude/skills/`. Tasks docs actualized to all-phases-accepted: ROADMAP (merged phases+milestones table, "Toward v0.1.0"), TASKS (walks closed, single history table), BUGS (fixed sections rolled off, chunk-size watch closed by the code-split), BACKLOG (open/completed split; real sprites raised to P1 as the v0.1.0 asset gate).

## 2026-08-17 — Phases 5+6 (Sound + Settings + Barn juice; Presets + Handoff) (fleet session "Phase 5+6 push")

Directed by the primary agent under Marty's remote instruction. App version: v0.0.0004 unchanged this session — the shared bump for Phases 5+6 landing together follows the v0.0.0004 precedent and awaits the Task Manager. All gates green after audit fixes: `npm run build` PASS, `cargo clippy -- -D warnings` PASS, `cargo test` 88/88, invoke-name contract 23/23 commands, howler confined to `sfx.ts`. NOT committed. Adversarial audit at new scale — 5 lenses, 34 agents: 28 raw findings, 24 confirmed by skeptics, ALL 24 fixed in two passes (2 distinct MAJOR runtime classes: demo-stop stale-event replay + demo accumulation persistence; also assemble_done stale chime, preset-apply empty-graph guard, settings flush-on-quit; 15 minors incl. TOCTOU never-clobber via `File::create_new`, integer zoom ladder, pre-gesture cue drop, `.cmd` resolution for bare claude names); 4 deviations ratified (moo duck excludes tool group, detached ding, blue settings toggles per "blue is you", idle fidgets approximated). Pending Marty-only acceptance: Phases 3+4 manuals, plus the four new Phase 5+6 manuals.

| Agent | What they did |
|---|---|
| Code Lead | Authored the third frozen contract, `docs/_archive/contracts/PHASE56_CONTRACT.md` (Stage-0 seams pattern, zero-overlap ownership grid, 23-command invoke contract) — integration found ZERO cross-lane defects; ran the 5-lens/34-agent adversarial audit and ratified the 4 deviations |
| Core Coder | Stage 0 — laid every shared seam before the lanes started: howler@2.2.4 installed (plan §2 stack), `src/store/settings.ts`, `src-tauri/src/settings.rs` (read/write_app_settings via app_config_dir), `set_claude_override` seam in assemble.rs, `sfx.ts` API stub + all sfx call sites, `preset.rs`/`handoff.rs` stubs, lib.rs at 23 commands, `dialog:allow-save` capability, App.tsx mounts, `LogEvent.demo` tag |
| Sound Designer | Lane A — moved from spec to code: full `src/scene/sfx.ts` implementation, 14 placeholder cues + 3 tool-layer cues per SOUND_DESIGN.md §2b/§3 (ducking, cooldowns, read-burst throttle, voice pool, never-queue, detached mode, calm/mute/hidden gates); deleted 14 stale barn_*/ui_* wavs; wrote `docs/_archive/manuals/PHASE5_SOUND_TEST_MANUAL.md` |
| UI Coder | Lane B — `src/settings/SettingsModal.tsx`: master volume, Barn/Tool sound switches, Mute, Calm mode, claude binary path with live override, port + context dir display; wrote `docs/_archive/manuals/SETTINGS_TEST_MANUAL.md` |
| Barn Coder | Lane C — SNES juice pass: scarf flutter, anticipation frames, eyes-glance, session accumulation (paper stacks, ajar drawers), pooled dust puffs + squash frames, bounciest stop-trot payoff, waiting choreography (5s/30s/5min), calm-mode reduced motion, pause-when-hidden + idle FPS throttle, demo-event filtering + DEMO badge, HUD restyled to tokens; wrote `docs/_archive/manuals/PHASE5_JUICE_TEST_MANUAL.md` |
| Multifunctional Coder | Lane D — delivered Phase 6 whole: `preset.rs` + `handoff.rs` (preset_save/list/read/export/apply never-clobber; handoff_generate via the reused Windows-safe ClaudeRunner, handoff_write with GENERATED header), `src/preset/` + `src/handoff/` UI with confirmation modals + clipboard variants (Chat/Code/Design); wrote `docs/_archive/manuals/PHASE6_TEST_MANUAL.md`; 10+ tests |
| Product Analyst | Separately produced `docs/Brainstorm_Features.md` — 18 ranked post-Phase-6 ideas from 3 lenses plus a competitor scan |
| Administrator | Compiled this log, updated ROSTER.md (Code Lead impact 70→75 for the third zero-defect contract + 24/24 audit close; session notes on seven rows) |

**Idle by scope**: Tester (all four new manuals were authored by the lanes themselves in the Tester's established format), Task Manager, Librarian, File System Manager, 2D Artist — their doc passes were not run this session.

**Lazy/useless agents to flag for Marty**: none. Every working agent produced verifiable output; the contract discipline delivered zero cross-lane integration defects, and the audit→skeptic→fix chain closed all 24 confirmed findings before gate re-run.

## 2026-08-16 — Phases 3+4 (Assemble + Live hooks) + early Barn prototype (fleet session)

App version after session: **v0.0.0004** (Task Manager; shared bump for both phases). Phase 2 accepted by Marty at session start. All gates green after audit fixes: `npm run build` (tsc strict + vite), `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 60/60, invoke-name contract 14/14 commands verified. Phases 3+4 and the Barn prototype await Marty's manual acceptance walks.

| Agent | What they did |
|---|---|
| Code Lead | Wrote the frozen Phase 3+4+Barn contract (`docs/_archive/contracts/PHASE34_BARN_CONTRACT.md`) all four coders built against independently; then ran the adversarial audit and found 6 real defects — 1 CRITICAL (ClaudeRunner could not spawn on Windows: .cmd argument newline rejection + cmd /C truncation, empirically confirmed, invisible to all tests because they use the fake Runner), 1 MAJOR (Inspector optimistic-status race leaving nodes stuck busy), 4 minor — plus a stale-comment note; appended contract §8 Revisions after the fixes |
| Core Coder | Built the Rust backend: `assemble.rs` (FIFO queue, max-2 concurrency, Runner trait seam, prompt build per §6, root-confined atomic .md writes, `assemble://status` events), `hooks.rs` (preview/write with never-clobber merge), `hooks_server.rs` (axum on 127.0.0.1:4923, lenient POST /event, BarnEvent normalization, `barn://event` emit); 7 new commands registered; 37 new tests (suite 60/60); then confirmed and fixed all 6 audit defects — critically, re-plumbing the claude spawn to pipe the prompt over stdin, empirically verified against the real `claude -p` on this machine — and re-ran the full gate |
| Multifunctional Coder | Built the Phase 3+4 frontend: `src/store/events.ts` (BarnEvent ring buffer, resolveNodeId, idempotent event listener), `src/assemble/` (types + the five invoke wrappers), EventLog panel, HooksModal trust-boundary flow (preview diff → explicit approve → write), Inspector AssembleSection (assemble/refine/summarize/cancel with status badges), live-read pulse + assemble states on MemoryNodeCard — all invoke names byte-exact per contract |
| Barn Coder | Built the entire Barn prototype in `src/scene/` (started early per Marty's authorization): Pixi 8 iso scene (Barnlight-29 palette, 2:1 iso math, programmatic props — no binary assets), interruptible cow task queue with walk/trot/bob anims, BarnEvent→animation mapper per §8, offline DemoPlayer with graph-empty fallback, camera pan/zoom, StrictMode-safe lifecycle; plus `docs/_archive/superseded/BARN_PROTOTYPE.md` integration doc |
| UI Coder | Integrated everything: Canvas ⇄ Barn segmented toggle in App.tsx (React Flow stays mounted, Pixi mounts on demand), wired DemoPlayer through the events store (closing the Barn Coder's store-seam gap so demo events reach EventLog + canvas pulse), spec re-check fixes (EventLog header hit-target, banned fourth Silkscreen use), hardened HooksModal keyboard safety (Cancel holds focus) and consequence wording; clean boundary audit; flagged 4 real UX debts (demo events in the live ring, HUD colors, idle ticker, 1.25MB chunk) |
| Tester | Wrote three grounded manuals: `docs/_archive/manuals/PHASE3_TEST_MANUAL.md` (36 steps — assemble pipeline, concurrency, refine/summarize, error paths, cancel), `PHASE4_TEST_MANUAL.md` (35 steps — hooks trust boundary incl. keyboard safety and never-clobber, synthetic curl events, real claude session), `BARN_PROTO_TEST_MANUAL.md` (28 steps — toggle lifecycle, demo walkthrough, interruptions), all in the PHASE2 format with sign-off tables |
| Task Manager | Bumped version to v0.0.0004; updated all six docs/tasks/ files — M2 reached, M3/M4 gates open, M5 marked prototype-started-early; Sprint 2 closed, Sprint 3 opened; 9 fleet tasks logged Done; new P0 "Phase 3+4 manual acceptance walk"; 7 Product Analyst items pulled into BACKLOG; 4 residual risks logged in BUGS Watching, 6 audit fixes in Fixed |
| Librarian | TERMINOLOGY.md: 10 new rows (Barnlight-29, AssembleQueue, hooks pair, Runner trait, demo mode, event log, resolveNodeId…) and 11 updated rows (Assemble/Refine/Summarize now implemented, command count 7→14 with full list, three stores, planned markers removed), each verified against the code |
| File System Manager | FILE_GRID.md: 2 new doc rows + 16-row session audit of every non-doc file touched, with agent attribution and gate results; recorded the DESIGN_PROMPT.md deletion as Marty's own staged action; full-tree hierarchy scan clean; flagged package.json/package-lock.json as modified-but-unclaimed (resolved: the Code Lead audit confirmed the diff is only the pre-authorized pixi.js@8 install) |
| Product Analyst | Researched competitors and 2026 UX baselines; added 13 ranked P1–P3 items to docs/FEATURES.md with sources — key findings: no direct competitor pairs a graph editor with compile + live hooks; streaming assemble output + cancel + explicit job states are table stakes; token-cost display now expected; feed needs retention cap + adopt-unmapped-read loop |
| Sound Designer | SOUND_DESIGN.md §2b: Phase-5 cue sheet keyed to the shipped scene code — 11 BarnEvent rows mapping exact mapper.ts/cow.ts hook points to named cues with lengths, explicit SILENT list with rationale, ducking/voice-pool rules, calm-mode single-gate design, howler sprite-map skeleton |
| 2D Artist | ART_DIRECTION.md: production sprite plan — 18 sheets with sizes/frame counts, runtime-tinted accent-tab scheme, Aseprite/packing conventions; verified palette byte-identical to palette.ts; flagged 7 placeholder deviations in src/scene/ to fix in the sprite pass (role hue on structure, baked-in dev figure, missing corkboard, floor dither rule…) |
| Administrator | Compiled this log, updated ROSTER.md (four agents off Idle; Code Lead impact 60→70 for the second frozen contract + critical-catch audit) and the CLAUDE.md Status line |

**Idle by scope**: none — every roster agent had in-scope work this session and delivered.

**Lazy/useless agents to flag for Marty**: none. All twelve working agents produced verifiable output; the audit→fix→verify chain caught and closed a defect that would have broken Assemble entirely on Windows despite a fully green test suite.

## 2026-08-16 — Phase 2: Compile (ultracode session)

App version after session: **v0.0.0003** (set by Task Manager). All automated checks green: `npm run build`, `cargo clippy --all-targets -- -D warnings`, `cargo test` 23/23, IPC contract verified. Phase 2 awaits Marty's manual acceptance walk.

| Agent | What they did |
|---|---|
| Code Lead | Wrote the frozen Phase 2 implementation spec (edge semantics, command contract, adapter rules) both coders built against without talking to each other; later ran an adversarial audit of compile.rs and caught 3 real medium defects (glob-dir aliasing that could silently destroy a root AGENTS.md, escaping globs crashing the whole preview, unescaped YAML in .mdc frontmatter) |
| Core Coder | Implemented `src-tauri/src/compile.rs`: serde graph model, full validation (cycles with reported path, missing files, dangling edges), Kahn topological ordering with readOrder tie-break, all three adapters (claude/agents/cursor), `compile_preview`/`compile_write` commands with write allowlist + header requirement, 13 unit tests; then confirmed and fixed all 3 audit findings and added 3 more tests (16 total) |
| Multifunctional Coder | Built `src/compile/` — wire types, invoke wrappers, hand-rolled LCS line diff with hunks, CompileModal (loading/errors/preview/writing/done/failed), target chips persisted to the graph store, Compile button in the TopBar |
| UI Coder | Re-checked the compile UI against DESIGN_SPEC and app idiom; fixed 7 issues, including a real behavior bug where post-compile rescan unmounted the whole workspace, plus custom checkbox, dialog focus/aria, keyboard-reachable collapse, empty-state text |
| Tester | Ran the full verification gate (tsc, clippy, cargo test, invoke-name contract check — all pass); wrote `docs/_archive/manuals/PHASE2_TEST_MANUAL.md`, a 51-step manual test script naming the real controls, covering happy path, validation, safety and a Phase 1 regression pass |
| Task Manager | Created `docs/tasks/` — ROADMAP (7 phases with acceptance criteria), TASKS (build-day log + open P0 "Phase 2 manual acceptance walk"), BACKLOG (12 real items), BUGS (4 fixed today preserved, 1 watching), SPRINTS, MILESTONES; defined version scheme v0.0.NNNN, current v0.0.0003 |
| Librarian | Updated `docs/TERMINOLOGY.md`: refreshed stale compile-related rows and added the new Phase 2 terms (clean folder glob, effective-pinned, errors-XOR-files, write allowlist, yaml_scalar, alwaysApply, unified diff/LCS…), verifying each against the actual code |
| File System Manager | Created `docs/fleet/FILE_GRID.md` — registry of all 18 documentation .md files with hierarchy rules; flagged 3 violations: V1 uncommitted deletion of docs/DESIGN_PROMPT.md still cited by two docs, V2 stale two-line README.md, V3 stale citation inside TERMINOLOGY.md header |
| Administrator | Compiled this log and ROSTER.md. (Note: the fleet-session Administrator run hit a session limit mid-start; the primary session completed the deliverables from the recorded reports.) |

**Idle by scope (not lazy)**: Product Analyst, Barn Coder, Sound Designer, 2D Artist — Phase 2 had no work in their lanes.

**Lazy/useless agents to flag for Marty**: none. Every agent with in-scope work delivered; the two review agents each found real, confirmed defects rather than rubber-stamping.
