`2026-08-17 01:40`

> **Triaged 2026-08-17** — all 18 ideas accepted into `docs/tasks/BACKLOG.md` ("Triaged from docs/Brainstorm_Features.md" section), grouped as quick wins / moat bets / platform bets with dependency cross-links. This file stays as the ranking record; the backlog copy is the working list.

| #                  | Name                               | Priority           | Tags                       | Description                                                                                                                                                                                                                                                                                                                        |
| ------------------ | ---------------------------------- | ------------------ | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0                  | Reality Check (drift lint)         | P1                 | drift, lint, git           | Static lint that checks every file path, command, script and port cited in a node against the live repo, with an optional claude -p pass for semantic claims (versions, stated behavior); broken claims get a red drift badge and a Problems entry quoting the exact lie. Runs on project open and after git operations.           |
| 1                  | Context audit changeset            | P1                 | heatmap, prune, tokens     | Turns N sessions of hook data into one applyable changeset — unpin never-read nodes, adopt unmapped hot files, split partially-read rules — with each line quoting its weekly token cost (sessions-per-week times pinned tokens); reviewed and applied like a compile diff, atomically.                                            |
| 2                  | Memory Inbox                       | P1                 | memory, curation           | Ingests Claude Code auto-memory (MEMORY.md) as an inbox lane on the canvas; one click promotes a machine-written fact into a real git-synced Memory Node or dismisses it, flagging duplicates and contradictions with existing nodes. Also shows which memories exist only on this machine, since promoted nodes sync via git.     |
| 3                  | cowtext check (CI drift gate)      | P1                 | ci, cli, teams             | Verify-only CLI plus a published GitHub Action that fails a PR when generated CLAUDE.md, AGENTS.md or .cursor/rules no longer match graph.json, were hand-edited (header hash), or contain drift-lint breakage, posting a graph-level diff as the PR comment. Makes the graph canonical by policy, not discipline.                 |
| 4                  | The moo is the notification        | P1                 | audio, ambient, opt-in     | Opt-in setting: while the app is hidden or minimized, only the turn-complete happy moo (plus an optional low moo on tool errors) still plays quietly, making the barn the user's Claude-is-done sound. Everything else stays silent when hidden.                                                                                   |
| 5                  | Branch-aware graph                 | P1                 | git, branches              | Watches .git/HEAD per project; on checkout reloads graph.json from the new branch, shows the branch in the title bar, and warns via the GENERATED header hash when the CLAUDE.md on disk was compiled from a different branch, offering one-click recompile.                                                                       |
| 6                  | Transcript mining for context gaps | P1                 | sessions, gaps             | Opt-in, local-only parsing of ~/.claude project transcripts to find context failure signatures — the agent grepping for facts an unpinned node already contains, re-reading a file 4+ times, asking questions a node answers — each surfaced as a gap card linking the transcript moment to a one-click fix (pin, edge, new node). |
| 7                  | Fleet dashboard                    | P1                 | multi-project, home        | Home screen listing every known project as a status card: hook health, drift-lint count, last session time, pinned-token total, unreviewed unmapped reads; a tray badge sums problems across the fleet. An optional farmstead skin renders each project as a small barn with its cow's current state.                              |
| 8                  | Barn Raising                       | P1                 | progression, retention     | Barn size and furnishing derive deterministically from real project history — node count adds cabinets, total sessions weather the wood, git age adds a loft or second wing — so a year-old project opens to a big lived-in barn and a fresh one starts as a single stall. No XP, no unlocks, no notifications.                    |
| 9                  | Named calves                       | P1                 | multi-agent, identity      | Each subagent name/type hashes to a stable calf appearance — coat patch pattern plus a tiny prop like the audit calf's spectacles — identical across sessions, so a six-agent fleet reads as six recurring distinguishable characters instead of interchangeable sprites.                                                          |
| 10                 | cowtext-mcp                        | P1                 | mcp, runtime, moat         | Bundled MCP server exposing Memory Nodes as resources/tools so any agent (Claude Code, Cursor, Codex) pulls nodes on demand at runtime instead of eating the whole pinned set up front; role and conditional-glob metadata become retrieval hints, and hooks log which nodes were actually fetched.                                |
| 11                 | Skill Studio                       | P1                 | skills, authoring          | Author skills, agents and commands as graph nodes, with a trigger simulator — type a hypothetical prompt and see which skills would load and what they cost in tokens — plus one-click export as an installable Claude Code plugin bundle. Extends the read-only .claude adoption of 1.9 into full authoring.                      |
| 12                 | Barn mission control               | P2                 | multi-agent, sessions      | Concurrent Claude Code sessions each get their own stall and full cow (peer, not calf) with per-session color lanes in the event log and tinted canvas pulses; a chalkboard by the barn door lists active subagents with task brief and working/done/stalled ticks, a stalled calf sitting down by its chalk line.                 |
| 13                 | Context loadouts                   | P2                 | profiles, compile          | Named pinned-set profiles per project (frontend work, release, debugging), each a saved pin selection with readOrder overrides; switching recompiles, the header records the active loadout, and hook sessions are tagged with it so the usage heatmap can compare per-loadout.                                                    |
| 14                 | Merge sentry                       | P2                 | git, review-inbox          | After a pull, merge or rebase, diffs incoming commits against a path-to-node index and queues affected nodes into a review inbox (this merge touched 14 files under src/auth — auth-rules.md may be stale); items clear by re-verifying, which bumps the existing lastVerified, or by editing.                                     |
| 15                 | Dust and cobwebs                   | P2                 | staleness, heatmap, barn   | Nodes unread for many sessions gather visible dust and cobwebs on their barn cabinets and gently desaturate on canvas as lastVerified falls behind churn in the files they describe; heavily-read cabinets stay polished with worn floorboards. Clicking a dusty cabinet jumps to the node; purely ambient, no toasts.             |
| 16                 | Context packages                   | P2                 | teams, sharing, versioning | Export a subgraph (org TypeScript standards, security rules) as a versioned package other projects import by git URL or from another repo; imports render as a locked group with an approval-gated update diff on new upstream versions, and local edits fork the node with a visible diverged-from-upstream marker.               |
| 17                 | Cowtext as a plugin                | P2                 | distribution, hooks        | Package the hook configuration plus a small graph-respecting skill (report unmapped reads, suggest node updates) as an installable Claude Code plugin, so a teammate without the desktop app wires the hooks feed with one plugin install and the app detects and adopts it — replacing hand-edited settings.json onboarding.      |