# INPUT PROMPT

Here lives a dynamic prompt for using inside Claude Code.
Consider everything below as a new prompt every time. It may or may not change. You can't tell before you read.

=== Everything below considered as a prompt ===

Ideas

1. Node's file name shoulb be synced with it's name in Cowtext
2. If hooks are installed display it. Button shouldn't be visible if hooks are installed
3. Resizable left/right panels
4. Make windows with code editor/viewer wider
5. In Role popup you should display brief description about meaning of the role currently active
6. Add RU/EN localization support
7. Make Order number on nodes bigger and more visible
8. Recent projects on startup screen
9. Add dynamic context menu (right click). Each element which can has context menu has it's own set of buttons depending on the current state
10. "Reveal in File explorer" for nodes. In node context menu, on left panel, in context menu on right panel.
11. Background chill and calm meditative 16-bit music
12. Add "Run Claude" button. The button opens window with different launch parameters as checkboxes/dropdowns/etc, and a Launch button
13. Awaiting spinner with overlay above right panel when scanning/rescanning
14. Add ability to connect nodes
15. I used to separate frontend's actual code, layout and styles to different files (.ts, .html, .css). If it's possible, make it so
16. I wish creation of nodes' edges were more beautiful:
    * Connectors may be everywhere on halfs of perimeter of the node. Input connectors may be anywhere from top to left and then to bottom, while Output connectors may be anywhere from top to left and then to bottom
    * The point of connection of line and arrow should look better and smoother
    * Edges should be spilnes, not lines
    * Increase radius of trigger zone for snapping connecting line to an Input/Output
    * Ability to set edge's color
17. Brief input field should be resizable
18. Markdown panel should be dockable OR code editor/viewer may be a separate independent window
19. I need a comprehensive Sub-Agent management solution. I want to create/delete sub-agents, rename them, give nicknames, set priority and influence, set roles, create and see used tools, create and see used skills, edit and read duties description, each agent should have an avatar (and a mini-version for different lists, dropdowns, etc).
20. Think about Marketplace of presets